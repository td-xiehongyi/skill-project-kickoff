---
name: project-kickoff
description: "Use when a user wants to clarify an idea, start a software project, or plan a substantial new direction in an existing project, especially when goals, scenarios, scope, or acceptance remain uncertain. 适用于深入确认需求、制定分阶段启动方案与项目文档；不用于普通修复、单项代码修改或既定计划执行。"
---

# Project Kickoff 项目启动

把想法变成有依据、范围明确、可验收、可交接的启动方案。优先减少会导致做错方向的假设，而不是增加问题和文档数量。默认中文交流，跟随用户指定的语言。

## 使用边界

- 服务新软件项目和已有项目中的较大新方向。若实际请求只是修复、单项修改或执行既定计划，回到该任务，不强行启动访谈。
- 核心是需求、阶段计划和必要文档；原型是按需的验证环节，最小工程是可选交付。完成启动交接后结束，不持续管理后续开发。
- 不依赖 Grill Me、Superpowers、ui-prototype、联网或特定提问工具。借鉴单问题追问，不考查用户、不打分。
- 以当前用户请求和已有授权限定动作。确认需求不自动授权代码搭建；已有明确覆盖搭建范围和验收目标的授权直接沿用。提交、推送、安装和发布只按实际授权执行。

## 1. 先调查起点

读取用户提供的材料、项目规则、现有启动记录及相关需求与实现，按问题定向探索，不全仓库漫游。修改前识别并保留未提交工作。记录四类信息：当前事实（带证据）、文档声明、用户目标、未验证假设。

新项目先辨明目的：学习、作品展示、实际使用，或它们的优先关系。已有项目补充当前行为、希望改变的行为与兼容约束；代码说明现在做了什么，不能替用户决定未来做什么。局部函数不足以证明完整运行路径。

资料冲突时展示具体差异及影响，不自动选一方当作已确认需求。能通过资料回答的问题不再问用户。恢复已有讨论时先读记录、核对当前材料，只处理真正新增或变化的内容。

## 2. 一次解决一个关键问题

进入访谈时读取 [访谈策略](references/interview-strategy.md)。每轮包含：对上一回答的必要反馈、一个会改变当前方案的问题、必要时的少量选项与推荐理由。可用且适用时使用单问题交互工具，否则直接对话；发出问题后等待回答。

从具体场景、现有做法和成功结果开始，再收敛首版范围、主要流程、非目标、重大约束与验收。技术方案在相关需求足够明确后比较。用户已指定技术时先识别其理由，不把技术讨论当作强制前置步骤。

**探索中的回答先用于缩小不确定性。不要在关键目的或场景未明时，一边提问一边给出看似已定的功能清单、完整路线图或架构。** 若用户要求候选思路，可以给标明“候选、尚未确认”的简短比较，再回到当前关键问题。

对偏好说明取舍后尊重选择；对事实寻找证据；对未知提出低成本验证。质疑功能的必要性、替代办法、成本与失败后果时，按用户真实目的评价。用户知情坚持后记录决定，除非出现新证据，不循环劝退。

## 3. 收敛与保存

某主题已经能形成结论时，简短展示决定、理由及仍待验证的内容，取得对该结论的确认后保存到同一启动记录；用户回答已明确确认该结论时直接保存，不再重复确认。普通回答仅确认其直接表达的内容，不能扩展为对整套推荐的认可。

准备保存或编写文档时读取 [文档与交接约定](references/documents-and-handoff.md)。遵循当前模式与写入权限；用户要求先讨论不落盘时保留对话内记录，说明尚未保存，不声称可从文件恢复。

当前范围的目标、主要流程、非目标、重大约束和验收已明确，且不存在会推翻近期方向的未决问题时，结束追问，整理方案。已说明的低成本可逆默认值可以采用，但标为默认，不记作用户确认。不要为了填满提纲重问。

关键事实无法靠讨论解决时，形成验证任务：需验证的假设、最小办法、观察结果如何影响决定。此时状态是“待验证”，可以形成条件性草案或交接，但不能把依赖它的实施计划标为已就绪。无关部分仍可继续。

## 4. 原型与方案

当页面结构或关键交互决定用户流程且尚不明确时，先澄清相关场景，再按需交接原型验证。已有明确设计直接引用版本，不强制重做原型。原型交接和返回内容见文档约定。

ui-prototype 不可用时提供交接说明与待验证项，必要时用文字流程帮助选择；不要自动创建另一个 Skill、安装依赖或宣称已经制作原型。制作原型按用户实际请求执行。UI 的未知仅阻塞依赖该决定的部分。

整合已确认需求，比较真正影响成本或方向的少量可行方案，明确推荐依据。原型、实验或新回答改变旧决定时，标记受影响的决定与文档，针对变化重新确认，保留旧理由而不是静默覆盖。

形成最小文档集合并展示供用户审阅。近期获准阶段写具体任务；远期只写目标、依赖、交付、验收与停止点。文档完整不等于用户已批准实施。

## 5. 可选搭建与交接

仅在授权明确覆盖当前搭建范围与验收目标时搭建；缺少授权时提出具体可审阅的范围，不提前生成代码。已有授权无需重复索取。仅搭能启动并走通一个最小流程的工程，不创建未来功能占位目录。

使用现有项目实际配置确定命令，执行与范围匹配的验证。分别说明实现、自动检查、实际运行、UI 对照和交付检查的证据；只报告真正验证到的层级。模拟数据和未接通的路径明确标注。

最后交接：完成内容与文件入口、已确认决定、验证证据、未验证项、下一阶段目标和授权边界。只完成文档时就在文档交接处结束；搭建结束后不自行推进下一阶段。

## 维护验证

创建或修改本 Skill 时才读取 [行为验收场景](references/behavior-tests.md)。格式检查、只读情景模拟、真实项目试用和安装后的触发检查是不同证据，不互相替代。


